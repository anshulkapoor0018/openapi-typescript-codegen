{{>header}}
/**
 * @typedef {Object} IBaseResult
 * @property {string} url - URL of the API endpoint
 * @property {boolean} ok - Boolean
 * @property {string} status - Status Code of Response
 * @property {string} statusText - Status Description of Response
 * @property {T} body - Type of Result Interface
 */
export interface IBaseResult<T> {
    url: string;
    ok: boolean;
    status: number;
    statusText: string;
    body: T;
}
